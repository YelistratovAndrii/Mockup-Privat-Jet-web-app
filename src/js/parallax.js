window.onload = function () {
  const parallaxAll = document.querySelectorAll('.parallax-container');
  if (!parallaxAll) {
    return;
  }

  // window.addEventListener('scroll', handleScroll);

  // function handleScroll() {
  //   const offset = window.pageYOffset;
  //   parallaxAll.forEach(
  //     (el) => (el.style.backgroundPositionY = -offset * 0.83 + 'px')
  //   );
  // }
};
